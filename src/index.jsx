// index.jsx
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Import the actual App component

ReactDOM.render(
  <React.StrictMode>
    <App /> {/* Render the actual App component */}
  </React.StrictMode>,
  document.getElementById('root')
);
